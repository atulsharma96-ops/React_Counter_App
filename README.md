# React + Vite + Tailwind
📌 Project Overview

This project is built to understand and implement the useState hook in React.
The main objective of this project is to learn how state works in React and how variable values change dynamically using state.

🔹 Learning Objectives

Through this project, I learned the following concepts:
What state is in React
How the useState hook is used to manage state
How variable values change using state update functions
How React re-renders the UI when state changes

🔹 useState Explanation

The useState hook returns two values:
The current state value
A function to update that state

const [count, setCount] = useState(0);

count stores the current value
setCount is used to update the value of count

🛠️ Technologies Used

React JS
JavaScript
useState Hook

✅ Conclusion

This Counter Project helped me gain a clear understanding of React useState, state updates, and how variable values change in a React component. It strengthened my foundation in React state management.
